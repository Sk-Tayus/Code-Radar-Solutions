#include <stdio.h>
#include <math.h>

int main () {
    int a,b;
    scanf("%d",&a);
    if (b == pow(a,2)) {
        printf("y");
    }
    else {
        printf("ya");

    }
    return 0;
}