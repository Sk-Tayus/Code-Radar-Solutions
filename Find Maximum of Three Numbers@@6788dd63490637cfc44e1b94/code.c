#include <stdio.h>
int main() {
    int a;
    scanf("%d", &a);
    if (a>b) {
        if (a>c) {
        printf("%d",a);
        }
    } else if (b>c) {
        printf("%d",b);
    }else {
        printf("%d"c);
    }
    return 0;
}